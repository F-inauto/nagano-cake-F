<!--部分テンプレート-->
<%= render 'public/items/genre_search', genres: @genres %>

<%= form_with model: @item, url: admin_items_path, method: :get, local: true do |f| %>

<!--画像-->
<%= image_tag @item.get_item_image, size: "200x200" %>


<!--商品名-->
<h2><%= @item.name %></h2>
<p><%= @item.introduction %>></p>
<h3>¥<%= @item.add_tax_price.to_s(:dalimited) %>(税込)</h3>

<!--個数選択-->
<%= f.label "個数選択" %>
<!--<= f.hidden_field :title, :value => "楽しいRuby" %>-->
<!--<= f.select :quantity, [['いち',1], ['に',2], ['さん',3] %>-->
<!--<= f.select :quantity,*[1..3] %>-->


<!--カートに入れる-->
<%= f.submit "カートに入れる", class: "btn btn-success" %>

<% end %>