<h4>配送先登録/⼀覧</h4>
</div>

<%= form_with model: @address, url: addresses_path, method: :post, local:true do |f| %>

<tr>
<th>郵便番号(ハイフンなし)</th>
<td> <%= f.text_field :post_number, placeholder: "0000000" %></td>
</tr>
<tr>
<th>住所</th>
<td> <%= f.text_field :address, placeholder: "東京都渋⾕区代々⽊神園町0-0", :size =>"60" %></td>
</tr>
<tr>
<th>宛名</th>
<td><%= f.text_field :name, placeholder: "令和道⼦" %></td>
<td><%= f.submit '新規登録', class:"btn btn-primary" %></td>
</tr>
</tbody>
</table>
<% end %>


<tr>
<th>郵便番号</th>
<th>住所</th>
<th>宛名</th>
<th></th>
</tr>
</thead>
<tbody>
<% @addresses.each do |address| %>
<tr>
<td><%= address.post_number %></td>
<td><%= address.address %></td>
<td><%= address.name %></td>
<td><%= link_to '編集する', edit_address_path(address.id), class: 'btn btn-primary' %>
<%= link_to '削除する', address_path(address.id), method: :delete, class: 'btn btn-primary' %></td>
</tr>
<% end %>
</tbody>
</table>
</div>

</div>